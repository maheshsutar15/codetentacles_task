import { ConnectButton } from '@rainbow-me/rainbowkit';

const App = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        padding: 12,
      }}
    >
      <ConnectButton />
    </div>
  );
};

export default App;
